# ⚡ QuickLaunch

> 轻量、快速、无广告的 Windows 11 应用启动器 — uTools 的简洁替代品

![Platform](https://img.shields.io/badge/platform-Windows%2011-0078D4?logo=windows)
![Tauri](https://img.shields.io/badge/Tauri-2.x-FFC131?logo=tauri)
![React](https://img.shields.io/badge/React-18-61DAFB?logo=react)
![Rust](https://img.shields.io/badge/Rust-stable-orange?logo=rust)
![License](https://img.shields.io/badge/license-MIT-green)

---

## ✨ 功能一览

| 功能 | 说明 |
|------|------|
| 🚀 **热键唤起** | `Ctrl+Shift+F1` 随时呼出，失焦自动隐藏 |
| 📌 **Pin 主页** | 常用应用固定到主页，开箱即用 |
| 🔍 **智能搜索** | 支持全名、前缀、子串、首字母缩写（`vs` → Visual Studio Code） |
| 🪟 **Win11 风格** | Acrylic 毛玻璃 + 无边框圆角窗口 |
| 🎛️ **编辑模式** | 进入编辑模式后拖拽 Pin 排序，右键重命名或取消固定 |
| 🪶 **极轻量** | 包体 ~5MB，常驻内存 ~15MB，零广告零遥测 |

---

## 🚀 快速开始

### 环境要求

| 工具 | 版本 |
|------|------|
| Node.js | ≥ 18 |
| Rust | stable |
| Visual Studio Build Tools 2022 | MSVC + Windows SDK |

```powershell
# 安装 Rust
winget install Rustlang.Rustup

# 克隆并启动
git clone https://github.com/Christinaper/quicklaunch.git
cd quicklaunch
npm install
npm run tauri dev

# 生产构建
npm run tauri build
```

---

## ⌨️ 使用方式

| 操作 | 效果 |
|------|------|
| `Ctrl+Shift+F1` | 唤出 / 切换显隐 |
| 输入关键词 | 进入搜索模式（支持缩写，如 `vsc` → VS Code） |
| `↑` `↓` / `Tab` | 导航选项 |
| `Enter` | 打开选中应用 |
| `Esc`（搜索中） | 清空搜索，返回 Pin 主页 |
| `Esc`（主页） | 隐藏窗口 |
| 点击窗口外 | 自动隐藏 |
| 搜索结果 `☆` | 固定到 Pin 主页 |
| Pin 右键 | 重命名 / 取消固定 |
| Pin 点击「编辑」| 进入编辑模式，可拖拽排序 |

---

## 🏗️ 技术架构

```
quicklaunch/
├── src-tauri/
│   ├── src/lib.rs          # Rust 核心：应用扫描、热键、Win32 窗口管理
│   ├── capabilities/
│   │   └── default.json    # Tauri 2.x IPC 权限声明
│   └── tauri.conf.json     # 窗口配置（无边框、透明、置顶）
└── src/
    ├── hooks/
    │   ├── useApps.ts      # 应用索引 + 多层搜索（精确/子串/缩写/Fuse）
    │   └── usePins.ts      # Pin 管理（localStorage 持久化）
    └── components/
        ├── PinBoard.tsx    # Pin 主页（编辑模式、右键菜单、拖拽排序）
        ├── ResultList.tsx  # 搜索结果列表
        ├── SearchBar.tsx   # 搜索输入框
        └── Footer.tsx      # 底部提示栏
```

### 搜索优先级

```
用户输入 "vs"
    ↓
① 精确匹配     → 无
② 前缀匹配     → 无
③ 子串匹配     → 无
④ 首字母缩写   → "Visual Studio Code" (v·s·c → vs✓), "Visual Studio" (v·s → vs✓)
⑤ Fuse 模糊   → 兜底
```

---

## 🔮 路线图

- [ ] 自定义热键（设置界面）
- [ ] 从 .exe 提取真实图标
- [ ] 使用频率排序（常用应用优先）
- [ ] 内联计算器（输入 `2+2` 直接显示结果）
- [ ] 文件搜索（集成 Everything SDK）
- [ ] 系统命令（`lock` `shutdown` `restart`）
- [ ] 主题切换（浅色 / 深色 / 跟随系统）

---

## 📄 许可证

[MIT License](LICENSE)
